import { HoverCommentDirective } from './hover-comment.directive';

describe('HoverCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverCommentDirective();
    expect(directive).toBeTruthy();
  });
});
