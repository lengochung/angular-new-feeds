import { FormatStringLikePipe } from './format-string-like.pipe';

describe('FormatStringLikePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStringLikePipe();
    expect(pipe).toBeTruthy();
  });
});
